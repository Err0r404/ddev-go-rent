<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark" hx-boost="true">
    <div class="container">
        <a class="navbar-brand fw-bold" href="{{ path('app_home') }}">
            <img src="{{ asset('images/mask_bg-dark.svg') }}" alt="{{ app_name }} logo" width="25" height="25" class="d-inline-block align-text-top">
            {{ app_name }}
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('app_home') }}">
                        Home
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="javascript:;">
                        Catalog
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="javascript:;">
                        Contact
                    </a>
                </li>

                {% if not(app.user) %}
                    <li class="nav-item">
                        <a class="btn btn-sm btn-secondary border-secondary fs-6" href="{{ path('app_login') }}">
                            Sign In
                        </a>
                    </li>

                    <li class="nav-item">
                        <a class="btn btn-sm btn-outline-light fs-6" href="{{ path('app_register') }}">
                            Sign Up
                        </a>
                    </li>
                {% else %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
                            My Account
                        </a>

                        <div class="dropdown-menu dropdown-menu-end">
                            <a class="dropdown-item" href="#">
                                Profile
                            </a>

                            <a class="dropdown-item" href="#">
                                Orders
                            </a>

                            {% if is_granted('ROLE_ADMIN') %}
                                <div class="dropdown-divider"></div>
                                <a class="dropdown-item" href="{{ path('app_admin_home') }}">
                                    Admin
                                </a>
                            {% endif %}

                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item text-danger" href="{{ path('app_logout') }}">
                                Sign Out
                            </a>
                        </div>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>