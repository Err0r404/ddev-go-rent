{% extends 'security/base.html.twig' %}

{% block title %}{{ parent() }} – Reset password{% endblock %}

{% block body %}
    {{ form_start(form, {'attr': {'class': 'w-75 m-auto', 'hx-post': path('app_reset-password'), 'hx-target': 'body'}}) }}
    <h3 class="text-center fw-normal mb-5">Reset password</h3>

    {{ form_errors(form) }}

    {{ form_row(form.plainPassword.first) }}
    {{ form_row(form.plainPassword.second) }}

    <div class="my-5 d-grid gap-3">
        <button class="btn btn-primary btn-lg btn-block" type="submit">Reset my password</button>
    </div>

    {{ form_end(form) }}
{% endblock %}
