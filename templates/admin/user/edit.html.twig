{% extends 'admin/base.html.twig' %}

{% block title %}{{ parent() }} – Edit user {{ user }}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col">
            <h1>Edit User</h1>

            <div class="d-flex justify-content-between align-items-center my-3">
                <div>
                    <a class="btn btn-outline-primary" href="{{ path('app_admin_user_index') }}" hx-boost="true">Back to list</a>
                </div>

                <div>
                    <a class="btn btn-primary" href="{{ path('app_admin_user_show', {'id': user.id}) }}" hx-boost="true">Show</a>

                    <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteUserModal{{ user.id }}">
                        Delete
                    </button>

                    {% include('admin/user/_delete_form.html.twig') with {'user': user} only %}
                </div>
            </div>

            {{ include('admin/user/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
    </div>
{% endblock %}
